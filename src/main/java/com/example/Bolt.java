package com.example;

import net.minecraft.item.Item;

// Create a new class called 'Bolt' that extends the 'Item' class
public class Bolt extends Item {
    public Bolt(Settings settings) {
        // Call the constructor of the parent class 'Item' with the provided 'settings'
        super(settings);
    }
}
